server:
  port: 22415
  servlet:
    context-path: /xs_assistant/area
spring:
  application:
    name: xs-area
  data:
    redis:
#      host: 127.0.0.1
#      port: 5050
      jedis:
        pool:
          enabled: true
          max-active: -1
          max-wait: 8s
          max-idle: 100
          min-idle: 0
      timeout: 2400
      password: xs0628
      host: 10.10.100.105
      port: 6379
  datasource:
    url: jdbc:mysql://10.10.100.105:3306/xs?useUnicode=true&allowPublicKeyRetrieval=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=Asia/Shanghai&useSSL=false
#    url: jdbc:mysql://localhost:3306/xs?useUnicode=true&allowPublicKeyRetrieval=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: xiaoshuai0628
  banner:
    charset: UTF-8
    location: Banner/XS-Assistant-Banner.txt
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://10.10.100.88:12577/eureka
#      defaultZone: http://127.0.0.1:12577/eureka
  instance:
    prefer-ip-address: true

resilience4j:
  retry:
    instances:
      area-select-api:
        max-attempts: 3
        wait-duration: 1s
  circuitbreaker:
    instances:
      area-breaker-api:
        register-health-indicator: true #是否将熔断器的状态注册为健康检查指标，上报熔断器的健康状态
        sliding-window-size: 10 #用于计算失败率的滑动窗口大小
        sliding-window-type: TIME_BASED
        permitted-number-of-calls-in-half-open-state: 3 #当断路器处于half-open的状态下，允许最大调用次数为3
        wait-duration-in-open-state: 5s #当熔断器打开后，会等待5s的时间尝试进入half-open状态
        failure-rate-threshold: 20 #当失败率达到20%,断路器则会打开,阻止进一步的调用
        event-consumer-buffer-size: 10
############### Micrometer Tracing + Zipkin 链路追踪 ###############
management:
  tracing:
    sampling:
      probability: 1.0 #设置跟踪采样的概率为100%(1.0),这意味着应用程序处理的所有请求都被跟踪,如果是10%(0.1)则为10条请求只有1条跟踪
  zipkin:
    tracing:
      endpoint: http://10.10.100.105:9411/api/v2/spans
logging:
  pattern:
    level: ${spring.application.name:},%X{traceId:-},%X{spanId:-} #日志记录格式
mybatis-plus:
  mapper-locations: classpath:Mapper/*.xml